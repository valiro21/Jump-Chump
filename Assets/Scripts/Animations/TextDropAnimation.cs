using UnityEngine;
using System.Collections;

public class TextDropAnimation : MonoBehaviour {

	/*public string text;
	public float SpaceBetweenLetters;
	public GameObject Letter;
	Vector3 DefaultPosition;
	Quaternion DefaultRotation;
	GameObject[] Letters;

	void Awake () {
		for (long i = 0; i < text.size (); i++) {
			DefaultPosition = new Vector3 ( DefaultPosition.x + SpaceBetweenLetters, DefaultPosition.y, DefaultPosition.z );

			Letters[i] = Instantiate ( Letter, DefaultPosition, DefaultRotation );
		}

	}

	void Drop () {
		foreach (GameObject Letter in Letters) {

		}
	}*/
}
